<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

/**
 * quando for usar banco
 
 * $ci=& get_instance();
 * $ci->load->database(); 
 */

function toArray(array $array){
    return $array = $array[0];
}